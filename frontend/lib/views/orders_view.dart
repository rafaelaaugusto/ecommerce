import 'package:flutter/material.dart';

class OrdersViews extends StatelessWidget {
  const OrdersViews({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Minhas compras'),
      ),
    );
  }
}
